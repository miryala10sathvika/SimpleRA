LOAD A
PRINT A
UPDATE A WHERE phy == 0 SET chem = -1928
PRINT A
SORT A BY Stud_ID IN DESC
PRINT A
UPDATE A WHERE Stud_ID >= 12 SET phy = 0
PRINT A
DELETE FROM A WHERE phy == 0
PRINT A
INSERT INTO A ( phy = 12 )
PRINT A
PRINT A
INSERT INTO A ( phy = 12 )
PRINT A
SORT A BY Stud_ID IN DESC
DELETE FROM A WHERE phy == 12
PRINT A
SORT A BY Stud_ID IN DESC
SORT A BY Stud_ID IN ASC
INSERT INTO A ( phy = 12 )
SORT A BY Stud_ID IN ASC
PRINT A
